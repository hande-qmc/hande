sys = hubbard_real {
    electrons = 4,
    lattice = { {2, 0}, {0, 2} },
    ms = 2,
}

fci {
    sys = sys,
}
fci {
    sys = sys,
    lanczos = {
        sparse = false,
        direct = true,
    },
}
