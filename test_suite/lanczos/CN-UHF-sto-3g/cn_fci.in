sys = read_in {
    int_file = "INTDUMP",
    nel = 13,
    ms = 1,
    sym = 0,
}

fci {
    sys = sys,
    lanczos = {
        neigv = 40,
        sparse = false,
    },
}
--    SCF calculation produced by Q-Chem using:  
